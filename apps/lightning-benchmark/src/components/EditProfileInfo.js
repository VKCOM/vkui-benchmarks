export const EditProfileInfo = (props) => {
    return (
        <section className="slds-card EditProfileInfo__body">
            <div>
                Edit your profile info
            </div>
            <a {...props}>Edit</a>
        </section>
    )
}